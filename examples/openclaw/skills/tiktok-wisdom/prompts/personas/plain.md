# Plain — default persona (no overrides)

This persona file is intentionally empty. The base voice rules in
`voice-preamble.md` apply as-is.

Drop a different persona file here (e.g. `bangkok-architect.md`,
`startup-founder.md`) and pass `--persona <name>` to layer it ON TOP of
the base voice.
